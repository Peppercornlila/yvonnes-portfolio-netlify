// next.config.ts
import type { NextConfig } from 'next';

// Erweitere die NextConfig-Schnittstelle, um turbopack zu erlauben
const nextConfig: NextConfig = {
  experimental: {},
};

export default nextConfig;
